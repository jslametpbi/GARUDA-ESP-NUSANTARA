# GARUDA ESP NUSANTARA — Final V8 Admin Entry Fixed

## Admin PIN
`JS2026`

## Final V8 fixes
- Admin Login button now forces the page to enter the Admin dashboard.
- Enter key in the Admin PIN field now works.
- Fixed the issue where the landing page stayed visible after successful login.
- Fixed duplicate Welcome popups.
- Added a hard CSS visibility rule so the landing page cannot override the hidden state.
- Old service-worker cache is cleared automatically.

## Deploy
Upload all extracted files to the GitHub repository root. After deployment, open the URL in an incognito/private window once or refresh twice to release any old cached version.
